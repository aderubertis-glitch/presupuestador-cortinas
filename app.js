const $=id=>document.getElementById(id);const state={lines:JSON.parse(localStorage.getItem("presupuesto_lines")||"[]"),priceMode:localStorage.getItem("presupuesto_price_mode")||"conIVA"};const esc=s=>String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));const money=n=>new Intl.NumberFormat("es-AR",{style:"currency",currency:"ARS",maximumFractionDigits:0}).format(n||0);const num=v=>Number(String(v||"").trim().replace(/\./g,"").replace(",","."))||0;const price=i=>state.priceMode==="conIVA"?i?.precioConIVA||0:i?.precioSinIVA||0;const find=(a,d)=>a.find(x=>x.descripcion===d)||null;function fill(){mecanismosList.innerHTML=PRODUCTOS.mecanismos.map(x=>`<option value="${esc(x.descripcion)}">`).join("");telasList.innerHTML=PRODUCTOS.telas.map(x=>`<option value="${esc(x.descripcion)}">`).join("");accesoriosList.innerHTML=PRODUCTOS.accesorios.map(x=>`<option value="${esc(x.descripcion)}">`).join("")}function calc(){const m=find(PRODUCTOS.mecanismos,mecanismo.value),t=find(PRODUCTOS.telas,tela.value),a=find(PRODUCTOS.accesorios,accesorio.value),w=num(ancho.value),h=num(largo.value),q=Math.max(1,+cantidad.value||1),aq=Math.max(1,+cantidadAccesorio.value||1);const mt=m?w*price(m)*q:0,tt=t?w*h*price(t)*q:0,at=a?(a.unidad==="ML"?w*price(a)*aq*q:price(a)*aq*q):0,total=mt+tt+at;mecanismoTotal.textContent=money(mt);telaTotal.textContent=money(tt);accesorioTotal.textContent=money(at);lineTotal.textContent=money(total);const warnings=[];if(t?.anchoMaximo){const x=Number(String(t.anchoMaximo).replace(",",".").match(/[\d.]+/)?.[0]);if(x&&w>x)warnings.push(`El ancho supera el ancho informado de la tela (${t.anchoMaximo}).`)}warning.hidden=!warnings.length;warning.innerHTML=warnings.map(x=>"⚠️ "+esc(x)).join("<br>");return{m,t,a,w,h,q,aq,total}}function save(){localStorage.setItem("presupuesto_lines",JSON.stringify(state.lines));localStorage.setItem("presupuesto_price_mode",state.priceMode)}function render(){budgetLines.innerHTML=state.lines.length?state.lines.map((x,i)=>`<article class="line"><div class="line-head"><span>Cortina ${i+1}</span><span>${money(x.total)}</span></div><div class="line-detail">${x.q} × ${x.w.toFixed(2).replace(".",",")} m × ${x.h.toFixed(2).replace(".",",")} m<br>${esc(x.m)}<br>${esc(x.t)}${x.a?`<br>Accesorio: ${x.aq} × ${esc(x.a)}`:""}</div><div class="actions"><button class="edit" onclick="editLine(${x.id})">Editar</button><button class="delete" onclick="removeLine(${x.id})">Eliminar</button></div></article>`).join(""):'<p class="empty">Todavía no agregaste ninguna cortina.</p>';grandTotal.textContent=money(state.lines.reduce((s,x)=>s+x.total,0));itemCount.textContent=`${state.lines.length} ${state.lines.length===1?"cortina":"cortinas"}`;const iva=state.priceMode==="conIVA";priceModeText.textContent=iva?"Precios con IVA":"Precios sin IVA";withVatBtn.classList.toggle("active",iva);withoutVatBtn.classList.toggle("active",!iva)}function clear(keep=true){if(!keep){mecanismo.value="";tela.value=""}ancho.value="";largo.value="";cantidad.value=1;accesorio.value="";cantidadAccesorio.value=1;calc()}function add(){const c=calc();if(!c.m)return alert("Elegí un mecanismo de la lista.");if(!c.t)return alert("Elegí una tela de la lista.");if(c.w<=0||c.h<=0)return alert("Ingresá ancho y largo.");state.lines.push({id:Date.now(),m:c.m.descripcion,t:c.t.descripcion,a:c.a?.descripcion||"",w:c.w,h:c.h,q:c.q,aq:c.a?c.aq:0,total:c.total});save();render();clear(true);ancho.focus()}function removeLine(id){state.lines=state.lines.filter(x=>x.id!==id);save();render()}function editLine(id){const x=state.lines.find(y=>y.id===id);if(!x)return;mecanismo.value=x.m;tela.value=x.t;ancho.value=String(x.w).replace(".",",");largo.value=String(x.h).replace(".",",");cantidad.value=x.q;accesorio.value=x.a;cantidadAccesorio.value=x.aq||1;removeLine(id);calc();scrollTo({top:0,behavior:"smooth"})}function copyLast(){const x=state.lines.at(-1);if(!x)return alert("Todavía no hay una cortina para copiar.");mecanismo.value=x.m;tela.value=x.t;ancho.value=String(x.w).replace(".",",");largo.value=String(x.h).replace(".",",");cantidad.value=x.q;accesorio.value=x.a;cantidadAccesorio.value=x.aq||1;calc();scrollTo({top:0,behavior:"smooth"})}function mode(m){if(state.lines.length&&!confirm("Al cambiar el tipo de precio se vaciará el presupuesto. ¿Continuar?"))return;state.priceMode=m;state.lines=[];save();render();calc()}function nuevo(){if(!state.lines.length||confirm("¿Empezar un presupuesto nuevo?")){state.lines=[];save();render();clear(false)}}function whatsapp(){if(!state.lines.length)return alert("Agregá al menos una cortina.");const total=state.lines.reduce((s,x)=>s+x.total,0),msg=["PRESUPUESTO DE CORTINAS","",...state.lines.flatMap((x,i)=>[`Cortina ${i+1}`,`${x.q} × ${x.w.toFixed(2).replace(".",",")} m × ${x.h.toFixed(2).replace(".",",")} m`,x.m,x.t,x.a?`Accesorio: ${x.aq} × ${x.a}`:"",money(x.total),""]).filter(Boolean),`TOTAL: ${money(total)}`,state.priceMode==="conIVA"?"Precios con IVA":"Precios sin IVA"].join("\n");open("https://wa.me/?text="+encodeURIComponent(msg),"_blank")}["mecanismo","tela","ancho","largo","cantidad","accesorio","cantidadAccesorio"].forEach(id=>$(id).addEventListener("input",calc));addBtn.onclick=add;copyLastBtn.onclick=copyLast;whatsappBtn.onclick=whatsapp;newBudgetBtn.onclick=nuevo;withVatBtn.onclick=()=>mode("conIVA");withoutVatBtn.onclick=()=>mode("sinIVA");fill();render();calc();if("serviceWorker"in navigator)navigator.serviceWorker.register("service-worker.js").catch(()=>{});